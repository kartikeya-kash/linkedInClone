function Navbar() {
  return (
    <nav>
      <button>Home</button>
      <button>Profile</button>
      <button>Messages</button>
      <button>Settings</button>
    </nav>
  );
}

export default Navbar;
